---
name: huashu-tiktok-id-script
description: 为 TikTok 印尼站电商拆解参考视频并创作印尼语短视频脚本，输出时间、画面、字幕、台词、声音及中文执行说明。适用于印度尼西亚 TikTok/TikTok Shop 的自然种草、达人合作、商品广告素材和短视频引流 LIVE；支持本地视频、TikTok 链接或已有拆解报告，并进行节奏、口语感、新鲜感、结构审稿改写。
---

# TikTok 印尼站：视频拆解与脚本创作

根据花叔 `huashu-douyin-script` 的六步流程改编，供 Codex 使用；这是独立适配版，不代表原作者或 TikTok 官方发布。原版及来源说明见 [来源与平台核对](references/platform-sources.md)。

默认面向印尼市场，以 Bahasa Indonesia 写片内字幕和台词，以中文写拍摄说明与分析。产品、人群和语气以本次用户资料为准，不自动套用 ONEBEAR。用户要中文版本时同步提供；其他市场需求应调整市场设定，不能沿用印尼规则。

## 1. 明确本次任务

先从对话和文件取得品牌、产品事实、目标受众、内容目标、目标时长和实际转化入口。存在影响成片的事实冲突才追问；价格、折扣、评论、播放成绩缺失时省略这些内容，不编数字补齐。

可接受 TikTok 链接、本地视频、已有分析报告或仅产品资料。只有产品资料时直接写原创方案，标记“待验证创意”。仅有报告时保留报告来源和精度，不声称重新看过视频。

按目标选择内容路线（以下时长为创作起点，可调整，不是平台限制）：

| 路线 | 主要任务 | 起步时长 | 结尾 |
|---|---|---|---|
| 自然种草 / POV / 教程 | 用生活场景与实物演示解释一个问题 | 25–35 秒 | 一个收藏、评论或商品详情动作 |
| 达人合作 / Affiliate | 用符合创作者语气的展示建立理解 | 20–35 秒 | 已核实可用的商品入口；执行商业内容披露 |
| 商品广告素材 | 尽早展示产品、兑现钩子、交代购买信息 | 15–30 秒 | 与真实落地页一致的商品 CTA |
| 短视频引流 LIVE | 给观众一个进直播间的具体理由 | 15–25 秒 | 仅在开播时间/入口可核实时引导 LIVE |

“千川”不直接翻译成 TikTok 广告产品名。涉及投放交付时核对当前 Seller Center/Ads Manager 的印尼账户能力；TikTok Shop 销售素材按实际 GMV Max/商品或 LIVE 场景组织，Spark Ads 是使用获授权帖子的方式，不是与 GMV Max 平行的内容类型。该 skill 只制作和整理内容，是否发布、投放、写飞书由本次任务授权决定。

## 2. 取得可读视频

`SKILL_DIR` 是本文件所在绝对目录。已有本地视频直接使用。需要下载 TikTok 参考视频时，使用现有 `yt-dlp`，辅助脚本支持主站视频页、`vm`/`vt` 短链和分享文本：

```powershell
python "SKILL_DIR/scripts/download_tiktok.py" --urls "TIKTOK_VIDEO_URL" --output-dir "WORK_DIR/tiktok-downloads"
```

优先匿名下载，只有确实需要且已有授权时才指定 `--cookies-browser chrome`（也可 edge/firefox）。保持现有网络代理和 TLS 校验；不要求登录国内抖音，不承诺无水印。遇到登录、地区不可用或链接失效时说明原因，使用用户能正常访问的本地视频，不反复尝试绕过限制。

## 3. 七维拆解，记录证据精度

分析视频前读取 [分析提示词](references/analysis-prompt.md)。保留钩子、分镜、节奏、视觉、转化、事实/平台核对、可迁移结构七个维度。特别区分可见事实、可听内容与创作推断。

**默认全部由 Codex 执行，不因“附件中有视频”就自动调用 Gemini。** 按下面顺序选择：

1. 没有视频：Codex 根据产品资料/已有报告直接完成结构、脚本、翻译、分镜和审稿；不调用 Gemini。
2. 有视频但任务只需写作、改写、翻译、审稿或整理已有拆解：仍由 Codex 完成，不调用 Gemini。
3. 需要视频事实：先让 Codex 利用当前可读的视频能力、已有报告、本地时间戳截图以及已取得的转写/声音信息。能满足任务时不调用 Gemini。
4. **只有必要的视频证据仍缺失**，例如连续动作、创作者口播与配乐人声区分、声画同步关系，而 Codex 当前能力无法可靠判断时，才调用 Gemini 做所需片段/问题的补充分析。调用前简短说明缺哪项证据及为何需要视频；用户已授权的范围内执行，不把常规分镜写作视为调用理由。不要为“可能更好”进行重复分析。

Gemini 只返回补充证据，后续脚本和四维审稿回到 Codex。当前授权允许上传且上面的条件满足时，使用辅助脚本；无视频或证据已足够的流程没有密钥依赖：

```powershell
python "SKILL_DIR/scripts/analyze_video.py" --video "LOCAL_VIDEO.mp4" --output "WORK_DIR/analysis.md" --model "AVAILABLE_GEMINI_MODEL" --question "仅确认缺失的声音或动作证据，并给出时间戳"
```

脚本读取 `GEMINI_API_KEY` 或 `GOOGLE_API_KEY`，模型也可由 `GEMINI_MODEL` 指定；先选账户实际可用模型，不把旧 preview 名称当永久可用。用 `--question` 限定必要问题，已知时段时用 `--start/--end` 限定模型分析时间；时间参数不减少上传原文件的范围，若只允许发送片段，应先在本地截取再上传。默认官方 Gemini 端点，额外依赖 `google-genai` 仅在大文件上传时需要。`--dry-run` 只检查参数，不上传视频、不读取密钥。遇到 401/403/429 或超时停止本次调用，保留已有分析继续写作；不要自动换付费模型或循环消耗额度。

开头 0–3 秒至少分别检查 0.0、0.5、1.0、1.5、2.0、2.5 秒画面，记录动画文字是否尚未出现完整。可用本地 FFmpeg 截帧，再逐张查看；模型抽样或半秒截图不能称为源视频逐帧核验。声音分析要区分创作者口播、配乐人声、字幕和环境音。

本机没有原生完整音视频入口时，Codex 可综合时间戳画面＋可用本地转写＋可获取的声音信息。明确哪些信息未取得；不把这种模式等同于完整音视频原生理解。用户明确禁止 Gemini 时保留缺失项，不擅自启用。

## 4. 提炼可测试的结构

输出“观察证据 → 可能起作用的机制 → 本产品如何重拍 → 需要验证的指标”。单个参考视频只能给结构启发，不能证明爆款因果。多个样本有共性也不自动等于高转化。

无参考视频时，从 [印尼语风格与结构样例](references/id-writing-and-examples.md) 选合适结构，标记为原创起点。这些样例没有实际发布成绩，也不按钩子效果排名。只迁移拍法、节奏和信息组织；不复制他人的素材、声音、品牌、身份、销量或使用体验。

将发现保存在任务目录的分析记录中。只有有明确数据来源、观察窗口和可比较条件时，才可标记已验证；不要每分析一条视频就自动改写 skill 内置参考库。

## 5. 生成印尼语成片制作表

生成前读取 [印尼语风格与结构样例](references/id-writing-and-examples.md)。按本地日常表达原创改写，不逐词翻译“姐妹们冲、老国货、千川、全网最低”等中文话术。

核心卖点选择 1–3 个，先做可见演示。前三秒呈现问题或结果预告，后文兑现；问句、无口播 ASMR、慢一点的证据镜头都可以使用。不强制价格锚定、赠品堆叠、焦虑激活或硬催单。

输出：

1. 内容简报：人群假设、单一目标、路线、时长、事实来源、待补素材。
2. 两个开头选项 A/B，选择一个贯穿成片；每个都有第一帧、字幕/台词和后文兑现动作。
3. 完整印尼语台词＋中文对照；台词按真人试读计时，不用中文字数估算印尼语时长。
4. 连续时间轴制作表，画面、字幕和台词分列：

| 镜号 | 时间 | 画面与动作（中文） | 字幕（ID） | 台词（ID） | 台词中文 | 声音与剪辑 |
|---|---|---|---|---|---|---|

5. 结尾与发布信息：只选一个行动；注明链接/LIVE/收藏评论的实际去向，以及商业内容披露、音乐来源和必要的素材核对。促销没提供就省略，不预填 `gratis ongkir`、`COD`、库存或 `Rp` 金额。
6. 拍摄/剪辑交接：道具与待实测项、9:16 构图、字幕安全区、声音留白、负责人待分配项。

字幕保持一屏一个意思，以实际手机观看和试读调整速度；避免挡住产品开口、面部、底部商品区域和右侧交互按钮。规则与账户入口会变化，需要发布适配时读取 [来源与平台核对](references/platform-sources.md)，核对当前官方页面。

## 6. 四维审稿并直接改写

| 维度 | 检查与修改方向 |
|---|---|
| 节奏 | 开头是否先展示承诺对象？是否及时兑现？删掉重复卖点，给装入、测量、拉合和字幕留真实时间。 |
| 口语感 | 印尼语是否能自然说出口？把产品手册句改成眼前动作；保留说话者惯用的少量口语词，不每句硬塞 slang。 |
| 新鲜感 | 是否只是“看看这个好物＋三个卖点”？更换问题、机位、演示顺序或信息揭示方式；不给每条脚本同样的惊叹开场。 |
| 结构 | 开头与正文是否同一问题？产品证据是否足够？结尾是否只要求一个能执行的动作？ |

附简短的“原句 → 问题 → 改写句”，把修改直接应用到完整台词和制作表，再检查一遍时间轴及事实一致性。除非用户明确要求逐轮审批，不为每轮审稿新增确认节点。

事实检查独立于风格检查：没有真实经历，不能用 `Aku sudah pakai sebulan` 伪装体验；不能把无依据的功效改成“我感觉有效”来规避要求。相同条件的真实演示可以描述，不把面料防泼水延伸成整包防浸水、电脑绝对安全。涉及健康、美妆、食品等品类时核对印尼市场及平台的当前类目要求；不预设 BPOM/halal 等资质或暗示认证。

## 交付与团队复用

默认完成制作表和可执行台词；用户要脚本库时增加脚本编号、版本、状态、负责人、计划日期、素材链接、发布链接、观察窗口、数据来源和下一版修改。初始状态“待试读/待素材/待实测”，实际指标留空。

相同产品做 A/B 时先只改一个主要变量；自然流量、广告和 GMV Max 汇总报告分开标识，不能直接当作同口径增量效果。按同账号、目标、时长与观察窗口比较，不预设通用“爆款阈值”。

只有用户要求写飞书时才使用当前可用的飞书 skill/连接器，并确认实际表与字段；本 skill 不硬编码既有库链接或团队成员。已有授权足够时完成写入并回读验证，不重复请求许可。

遵循用户的交付目录偏好，用日期时间避免覆盖旧文件。临时视频和分析留在任务工作区，最终成品单独交付；不要把密钥、Cookie 或完整请求体写进报告和技能包。
